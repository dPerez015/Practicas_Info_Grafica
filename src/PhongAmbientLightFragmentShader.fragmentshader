#version 330 core
out vec4 color;


in vec3 Normal;
in vec3 FragPos;

uniform vec3 ObjectColor;
uniform vec3 lightColor;

uniform float ambInt;

void main(){

	color=vec4(lightColor*ambInt*ObjectColor,1);
}